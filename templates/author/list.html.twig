<h1>List of Authors</h1>
Number d'auteurs = {{ x|length }}

{% set SUM = 0 %}

{% if x is empty %}
    <h4>Table is empty</h4>
{% else %}
    <table border="9">
        <tr>
            <th>Id</th>
            <th>Username</th>
            <th>nbrBooks</th>
            <th>show Details</th>
        </tr>
        {% for i in x %}
            <tr>
                <td>{{ i.id }}</td>
                <td>{{ i.username | upper }}</td>
                <td>{{ i.nbrBooks }}
                <td><a href="#">Details</a></td></td>
            </tr>
            {% set SUM = SUM + i.nbrBooks %}
        {% endfor %}
    </table>

    <h3>SUM Books = {{ SUM }}</h3>
{% endif %}
